name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: echo "Hello. Greetings."
      - name: Print Goodbye
        run: echo "done. goodbye."
